# VCT-16: Frontend service unit test

**Type:** task
**Status:** todo

Add at least one unit test for `MeetingsService`. Depends on VCT-10.

**Acceptance criteria:**
- [ ] `MeetingsService` has a unit test covering at least one HTTP call via `HttpClientTestingModule`
- [ ] `npm test` passes
